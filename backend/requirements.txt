# FastAPI Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9  # For file uploads
aiofiles>=24.1.0  # For async file operations

# LangChain Ecosystem
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langchain-openai>=0.2.0  # Optional: OpenAI fallback

# Blockchain & Web3
web3>=7.0.0  # Updated for 2025
eth-account>=0.13.0
eth-utils>=5.0.0
eth-hash[pycryptodome]>=0.7.0  # For Ethereum hashing
hexbytes>=1.2.0
# Note: safe-eth-py removed - using TypeScript Safe SDK in blockchain/ folder instead

# Database (PostgreSQL + Prisma)
prisma>=0.15.0  # Python Prisma Client
asyncpg>=0.29.0  # Async PostgreSQL driver
sqlalchemy>=2.0.35  # ORM (if needed alongside Prisma)
alembic>=1.13.0  # Database migrations
psycopg2-binary>=2.9.9  # Sync PostgreSQL driver (backup)

# Vector Storage & Embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0  # For embeddings
tiktoken>=0.7.0  # Token counting

# IPFS & Decentralized Storage
requests>=2.32.0  # For Pinata API calls
# Note: Using Pinata HTTP API instead of ipfshttpclient (more stable)

# Caching & Queue
redis>=5.1.0
celery>=5.4.0  # Optional: For background task queue
flower>=2.0.1  # Optional: Celery monitoring

# Security & Authentication
cryptography>=43.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4  # Password hashing
pyjwt>=2.9.0  # JWT tokens

# Utilities
httpx>=0.27.0  # Async HTTP client
websockets>=13.0
python-dotenv>=1.0.0
python-dateutil>=2.9.0
pytz>=2024.1

# Monitoring & Logging
loguru>=0.7.2  # Better logging
sentry-sdk>=2.14.0  # Error tracking (optional)

# Development & Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
black>=24.8.0  # Code formatting
ruff>=0.6.0  # Linting
mypy>=1.11.0  # Type checking

# Optional: Performance
orjson>=3.10.0  # Faster JSON serialization